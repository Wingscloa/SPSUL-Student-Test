:root {
    --solid-border: #cccccc;
    --brand: #ff9000;
    --text: #222;
}

* {
    margin: 0;
    box-sizing: border-box;
}

body {
    background: #FAFAFA !important;
}

#testContainer {
    height: 80vh;
    width: 75%;
}

.header {
    font-size: 24px;
    font-family: Myfont;
    font-weight: 500;
}

#testRange{
    width: 37vh;
}

    #testRange[type='range'] {
        -webkit-appearance: none !important;
        background: white;
        height: 12px;
        border: orange solid 1px;
        -moz-border-radius: 6px;
        -webkit-border-radius: 6px;
        border-radius: 6px;
    }

    #testRange[type='range']::-webkit-slider-thumb {
        -webkit-appearance: none !important;
        background: orange;
        height: 28px;
        width: 28px;
        border-radius: 32px;
    }

.questContainer {
    padding: 16px;
    border: 2px solid var(--solid-border);
    min-height: 20vh;
    width: 480px;
    border-radius: 14px;
    box-shadow: 2px 5px 15px 1px rgba(0, 0, 0, 0.1);
}

.questionHeader {
    font-size: 22px;
    font-weight: 500;
    font-family: Myfont;
    margin-bottom: 12px;
    color: rgb(255,144,0);
}

.scrollOverflow {
    overflow: auto;
    min-width: 0;
    -ms-word-break: break-word;
    word-break: break-word;
}

.questionName {
    font-size: 20px;
    font-family: Myfont;
    max-height: 200px;
}

.radioInput{
    color: black;
}

/* RADIO GROUP STYLING */
.radio-group { display: grid; gap: 14px; margin-top: 12px; }
.radio-option {
    position: relative;
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 14px 18px 14px 20px;
    border: 2px solid var(--solid-border);
    border-radius: 18px;
    background: #fff;
    cursor: pointer;
    transition: border-color .15s ease, box-shadow .15s ease, background .15s ease;
}
.radio-option:hover { border-color: #bdbdbd; }
.radio-option:has(input:focus-visible) { box-shadow: 0 0 0 3px rgba(255,144,0,.25); }

.radio-option input[type="radio"] {
    appearance: none;
    -webkit-appearance: none;
    width: 28px; height: 28px;
    border-radius: 50%;
    border: 2px solid #9e9e9e;
    outline: none;
    display: grid; place-items: center;
    background: #fff;
    transition: border-color .15s ease, box-shadow .15s ease, background .15s ease;
}
.radio-option input[type="radio"]::before {
    content: "";
    width: 14px; height: 14px;
    border-radius: 50%;
    transform: scale(0);
    background: var(--brand);
    transition: transform .15s ease;
}
.radio-option input[type="radio"]:checked { border-color: var(--brand); }
.radio-option input[type="radio"]:checked::before { transform: scale(1); }

.radio-label {
    font-family: Myfont, system-ui, sans-serif;
    font-size: 22px;
    font-weight: 500;
    color: var(--text);
}

/* compact note above group */
.radio-note { color: #7a7a7a; font-size: 14px; margin-bottom: 6px; }

