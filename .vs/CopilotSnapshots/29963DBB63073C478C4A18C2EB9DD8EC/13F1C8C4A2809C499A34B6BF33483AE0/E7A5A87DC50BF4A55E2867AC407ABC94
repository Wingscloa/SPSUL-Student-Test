:root {
    --brand: #ff8a00;
    --text: #222;
    --muted: #6b6b6b;
    --card: #fff;
    --border: #e9e9e9;
    --fail: #4b4b4b;
    --radius: 12px;
}

.tests-grid {
    display: grid;
    grid-template-columns: 420px 1fr;
    gap: 48px;
    align-items: start;
    height: 100vh;
}

.hero {
    display: flex;
    height: 100vh;
    -ms-flex-direction: column;
    -webkit-flex-direction: column;
    flex-direction: column;
    justify-content: space-between;
    align-content: end;
}

.hero-title {
    margin-top: 4rem;
    margin-left: 4rem;
    margin-bottom: 2rem;
    line-height: .9;
}

.hero-title-top {
    display: block;
    font-size: 92px;
    font-weight: 800;
    color: var(--text);
    margin-right: 40px;
}

.hero-title-bottom {
    display: block;
    font-size: 80px;
    font-weight: 800;
    color: var(--brand);
}

.btn-primary {
    display: inline-block;
    background: var(--brand);
    color: #fff;
    font-weight: 700;
    min-width: 200px;
    text-align: center;
    font-size: 18px;
    padding: 16px 28px;
    border-radius: 10px;
    margin: 64px 60px 32px;
    text-decoration: none;
    box-shadow: 0 8px 20px rgb(255 138 0 / 25%);
    transition: transform .15s ease, box-shadow .15s ease;
}

    .btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 12px 26px rgb(255 138 0 / 30%);
    }

.donut-wrap {
    margin-bottom: 7rem;
    margin-left: 4rem;
    width: 320px;
    aspect-ratio: 1 / 1;
    display: flex;
    align-items: center;
    flex-direction: column;
}

.donut-legend {
    display: flex;
    gap: 28px;
    margin-top: 10px;
    font-weight: 600;
}

.legend.success {
    color: var(--brand);
}

.legend.fail {
    color: var(--fail);
}

.content {
    display: flex;               /* změna na flex container */
    flex-direction: column;
    gap: 14px;
    padding: 32px;
    height: 100vh;                /* vyplní celý viewport */
    box-sizing: border-box;
    overflow: hidden;
}

.search {
    display: flex;
}

.search-input {
    width: 100%;
    padding: 12px 16px;
    border: 1.5px solid var(--brand);
    border-radius: var(--radius);
    outline: none;
}

    .search-input::placeholder {
        color: #a9a9a9;
    }

.table-scroll {
    background: var(--card);
    border-radius: var(--radius);
    border: 1px solid var(--border);
    overflow: auto;
    flex: 0 0 auto;               /* tabulka zabere jen svou výšku */
    max-height: 45%;              /* volitelný strop, uprav dle potřeby */
}

.tests-table {
    width: 100%;
    border-collapse: collapse;
}

    .tests-table thead th {
        text-align: left;
        font-weight: 700;
        color: var(--muted);
        padding: 14px 18px;
        border-bottom: 2px solid var(--border);
    }

    .tests-table tbody td {
        padding: 14px 18px;
        border-bottom: 1px solid var(--border);
    }

    .tests-table tbody tr:hover {
        background: #fff7ef;
    }

    .tests-table a {
        text-decoration: none;
        color: var(--muted);
    }

.stats-container {
    width: 100%;
    flex: 1 1 auto;               
    display: flex;
    align-items: stretch;
    justify-content: center;
    margin-top: 24px;
    min-height: 0;                

}

.stats-grid {
    display: grid;
    width: 100%;
    height: 90%;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: repeat(2, 1fr);
    gap: 16px;
    background-color: #eee;
    padding: 8px;
    border-radius: 8px;
    box-shadow: 0 2px 4px rgba(0,0,0,.25), 0 1px 2px rgba(0,0,0,.1);
}

.stats-item {
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    box-shadow: 0 2px 4px rgba(0,0,0,.25), 0 1px 2px rgba(0,0,0,.1);
    font-weight: 600;
    font-size: 18px;
    color: #222;
    cursor: pointer;
    backdrop-filter: blur(2px);
}

    .stats-item:hover {
        -moz-transform: scale(1.02);
        -ms-transform: scale(1.02);
        -o-transform: scale(1.02);
        -webkit-transform: scale(1.02);
        transform: scale(1.02);
    }

.stats-item--xl { grid-column: span 2; grid-row: span 2; }
.stats-item--wide { grid-column: span 2; }


.gridHeader {
    color: rgb(250,250,250);
    font-weight: 600;
    font-size: 2rem;
    text-align: center;
    padding: 1rem;
    -webkit-backdrop-filter: blur(2px);
    backdrop-filter: blur(2px);
    background-color: rgba(0, 0, 0, 0.5);
    -moz-border-radius: 8px;
    -webkit-border-radius: 8px;
    border-radius: 8px;
}

.bg-students {
    background-image: url('/Image/StudentsBlur.jpg');
    background-repeat: no-repeat;
    background-size: cover;
}

.bg-tests {
    background-image: url('/Image/TestsBlur.jpg');
    background-repeat: no-repeat;
    background-size: cover;
}

.bg-score {
    background-image: url('/Image/ScoreBlur.jpg');
    background-repeat: no-repeat;
    background-size: cover;
}

.bg-questions {
    background-image: url('/Image/QuestionBlur.jpg');
    background-repeat: no-repeat;
    background-size: cover;
}

@media (max-width: 1024px) {
    .tests-grid {
        grid-template-columns: 1fr;
    }

    .donut-wrap {
        width: 260px;
    }

    .table-scroll {
        max-height: 40%;
    }
}

@media (max-width: 900px) {
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
        grid-template-rows: repeat(4, 160px);
    }
    .stats-item--xl { grid-column: span 2; grid-row: span 2; }
    .stats-item--wide { grid-column: span 2; grid-row: span 1; }
}

/*Bootstrap breakpoints*/

@media(max-width: 992px){

    .desktop {
        display: none;
    }

    .donut-wrap {
        display: none;
    }

    .hero{
        max-height: fit-content;
    }

    .tests-grid {
        grid-template-columns: 1fr;
        gap: 0px;
    }

    .hero-title{
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        margin-right: 4rem;
    }

    .btn-primary {
        margin-top: 32px;
    }

    .content{
        padding-top: 16px;
    }

}